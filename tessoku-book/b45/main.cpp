#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    ll a, b, c;
    cin >> a >> b >> c;
    if (a + b + c == 0) cout << "Yes" << endl;
    else cout << "No" << endl;
    return 0;
}