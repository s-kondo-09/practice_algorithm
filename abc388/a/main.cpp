#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    string S;
    cin >> S;

    string ans = "";
    ans += S[0];
    ans += "UPC";

    cout << ans << endl;
    return 0;
}